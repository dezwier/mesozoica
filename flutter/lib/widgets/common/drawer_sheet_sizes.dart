/// Shared height fractions for catalog / filter / tree bottom drawers.
class DrawerSheetSizes {
  DrawerSheetSizes._();

  static const double initialChildSize = 0.90;
  static const double minChildSize = 0.35;
  static const double maxChildSize = 0.90;
}
